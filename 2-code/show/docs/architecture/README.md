# architecture 文档

## 架构总览

项目采用四层结构：

1. 算法层：`src/huashu_showcase/*`
2. 编排层：`scripts/*`
3. 资产层：`assets/*`
4. 文档层：`docs/*`

## 解耦思路

- 计算与绘图分离：算法模块不依赖具体文件输出。
- 参数与配置集中：机械臂常量统一在 `config.py`。
- 脚本薄封装：脚本只负责调用模块并落盘结果。

## 可扩展点

- 可新增 `benchmark/` 子包用于参数搜索。
- 可新增 `tests/` 做回归测试与性能基准。
